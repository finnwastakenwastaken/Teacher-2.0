opcache.enable = 1
opcache.enable_cli = 0
opcache.memory_consumption = 128
opcache.interned_strings_buffer = 16
opcache.max_accelerated_files = 20000

; Source never changes inside a production image, so skip the stat() calls.
; A deploy replaces the container, which discards this cache anyway.
opcache.validate_timestamps = 0
