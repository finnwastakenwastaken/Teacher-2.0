<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE policymap [
<!ELEMENT policymap (policy)*>
<!ATTLIST policymap xmlns CDATA #FIXED ''>
<!ELEMENT policy EMPTY>
<!ATTLIST policy xmlns CDATA #FIXED '' domain NMTOKEN #REQUIRED
  name NMTOKEN #IMPLIED pattern CDATA #IMPLIED rights NMTOKEN #IMPLIED
  stealth NMTOKEN #IMPLIED value CDATA #IMPLIED>
]>
<!--
  Defence in depth for App\Services\ImageOptimiser, against an accident, not
  a hostile user (uploading is auth-only, one account). ImageMagick sniffs
  format from content, and MediaLibrary::resolveMime() can fall back to a
  filename extension, so a mislabelled file could still reach a coder that
  turns "decode an image" into "follow instructions in a file" — MSL/MVG
  script, URL/HTTPS/FTP fetch over the network (SSRF), TEXT/LABEL/EPHEMERAL
  read or delete paths. None is a format this site accepts. SVG is disabled
  too even though the optimiser never passes it to ImageMagick, so a future
  caller can't reintroduce that path by accident.
-->
<policymap>
  <policy domain="delegate" rights="none" pattern="*"/>

  <policy domain="coder" rights="none" pattern="MSL"/>
  <policy domain="coder" rights="none" pattern="MVG"/>
  <policy domain="coder" rights="none" pattern="SVG"/>
  <policy domain="coder" rights="none" pattern="URL"/>
  <policy domain="coder" rights="none" pattern="HTTPS"/>
  <policy domain="coder" rights="none" pattern="HTTP"/>
  <policy domain="coder" rights="none" pattern="FTP"/>
  <policy domain="coder" rights="none" pattern="EPHEMERAL"/>
  <policy domain="coder" rights="none" pattern="TEXT"/>
  <policy domain="coder" rights="none" pattern="LABEL"/>
  <policy domain="coder" rights="none" pattern="SHOW"/>
  <policy domain="coder" rights="none" pattern="WIN"/>
  <policy domain="coder" rights="none" pattern="PLT"/>

  <!-- Indirect reads: `@file` syntax that makes ImageMagick open a path of
       the file's choosing rather than the one it was given. -->
  <policy domain="path" rights="none" pattern="@*"/>
</policymap>
